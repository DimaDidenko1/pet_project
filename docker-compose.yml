version: "3.8"

services:
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3333:3000"
    depends_on:
      - backend
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8888:8000"